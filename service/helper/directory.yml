---
- name: helper | directory
  block:
    - name: helper | directory | remove
      file:
        path: '{{ path }}'
        state: absent
    - name: helper | directory | create
      file:
        path: '{{ path }}'
        state: directory
