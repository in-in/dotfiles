---
- name: key
  become: yes
  apt_key:
    url: '{{ item }}'
  register: apt_key_result
  until: apt_key_result.failed is defined
  retries: 5
  delay: 10
  loop: '{{ utility_key_list | flatten }}'
