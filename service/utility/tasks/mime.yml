---
# Desktop entries folder:
# /usr/share/applications/ - system-wide
# ~/.local/share/applications - user-specific

- name: mime | create list
  template:
    src: 'mimeapps.list.j2'
    dest: '{{ user_config }}/mimeapps.list'
  vars:
    list: '{{ utility_mime_list | flatten }}'
