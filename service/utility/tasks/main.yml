---
- name: main | include all tasks
  include_tasks: '{{ role_path }}/tasks/{{ outer_item }}.yml'
  loop:
    - global_config
    - create_list
    - key
    - repository
  loop_control:
    loop_var: outer_item
