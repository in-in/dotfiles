---
- name: main | include all tasks
  include_tasks: '{{ role_path }}/tasks/{{ outer_item }}.yml'
  loop:
    - global_config
    - create_list
    - key
    - repository
    - git_repository
    - copy
  loop_control:
    loop_var: outer_item
