---
utility_repository:
  - repo: 'ppa:hluk/copyq'

package_apt_list:
  install:
    - 'copyq'

utility_copy:
  paths:
    - src: '{{ dotfiles_roles }}/copyq/files/copyq'
      dest: '{{ user_config }}'
