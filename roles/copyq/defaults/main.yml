---
utility_repository:
  - repo: 'ppa:hluk/copyq'

utility_package:
  apt:
    install:
      - 'copyq'

utility_copy:
  paths:
    - src: '{{ dotfiles_roles }}/copyq/files/copyq'
      dest: '{{ user_config }}'

startup_list:
  name: 'copyq'
  delay: 40
