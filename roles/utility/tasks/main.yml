---
- include_tasks: '{{ role_path }}/tasks/{{ outer_item }}.yml'
  with_items:
    - create_list
    - key
    - repository
    - package
    - pip
    - git_repository
    - link
    - gitignore
    - copy
    - startup
  loop_control:
    loop_var: outer_item
