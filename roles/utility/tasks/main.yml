---
- include_tasks: '{{ role_path }}/tasks/{{ outer_item }}.yml'
  loop:
    - create_list
    - key
    - repository
    - package
    - git_repository
    - link
    - copy
    - gitignore
    - alias
    - startup
  loop_control:
    loop_var: outer_item
