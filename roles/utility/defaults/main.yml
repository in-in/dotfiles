---
utility_task_list:
  - utility_copy
  - utility_git_repository
  - utility_gitignore
  - utility_key
  - utility_link
  - utility_package
  - utility_repository
  - utility_startup

utility_package:
  install:
    - curl
    - dconf-editor
    - fonts-hack-ttf
    # - glances
    - gnome-shell-extension-dash-to-panel
    - gnome-tweaks
    - synaptic
    - tree

gitignore_tmp: '/tmp/ignore_list'
utility_gitignore:
  ignore:
    - '*.retry'

startup_dest: '{{ user_config }}/autostart'
