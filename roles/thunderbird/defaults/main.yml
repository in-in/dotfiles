---
utility_package:
  apt:
    install:
      - 'thunderbird'

utility_copy:
  dir:
    - '{{ user_home }}/.thunderbird'
  paths:
    - src: '{{ dotfiles_roles }}/thunderbird/files/.thunderbird'
      dest: '{{ user_home }}'
