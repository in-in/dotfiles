- import_tasks: '{{ helpers }}/package_apt.yml'
  vars:
    package_apt_facts:
      install:
        - 'easytag'

- import_tasks: '{{ helpers }}/dconf.yml'
  vars:
    dconf_facts: '{{ easytag_settings }}'
  tags: 'dconf_easytag'
