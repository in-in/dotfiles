---
utility_link:
  - src: '/media/{{ ansible_user_id }}/{{ storage_ext_id }}'
    dest: '{{ user_home }}/storage_ext'
  - src: '{{ chapter27 }}'
    dest: '{{ user_storage }}'

filesystem_device_list:
  - '/dev/sda3'
  - '/dev/sdb1'
