---
- name: fill git config
  git_config:
    name: '{{ item.name }}'
    value: '{{ item.value }}'
    scope: '{{ item.scope | default("global") }}'
    repo: '{{ item.repo | default(omit) }}'
  with_items: '{{ git_config_data }}'
