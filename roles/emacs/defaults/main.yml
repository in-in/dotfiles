---
utility_repository:
  - repo: 'ppa:kelleyk/emacs'

utility_package:
  apt:
    install:
      - 'emacs25'

utility_link:
  src: '{{ dotfiles_roles }}/emacs/files/emacs.d'
  dest: '{{ user_home }}/.emacs.d'
