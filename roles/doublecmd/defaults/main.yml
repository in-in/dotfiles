---
utility_package:
  apt:
    install:
      - 'doublecmd-gtk'

utility_copy:
  dir: '{{ user_config }}/doublecmd'
  paths:
    - src: '{{ dotfiles_roles }}/doublecmd/files/doublecmd'
      dest: '{{ user_config }}'
