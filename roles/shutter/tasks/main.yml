---
- name: create directory
  include_tasks: '{{ helper_directory }}'
  vars:
    path: '{{ shutter.dest }}'

- name: create config
  template:
    src: 'settings.xml.j2'
    dest: '{{ shutter.dest }}/settings.xml'
