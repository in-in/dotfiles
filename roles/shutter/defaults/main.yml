---
# TODO: add Prt Sc + Shift hotkey

utility_package:
  apt:
    install:
      - 'shutter'

utility_template:
  - dest: '{{ user_home }}/.shutter'
    path: '{{ dotfiles_roles }}/shutter/templates'
    filename: 'settings.xml'
