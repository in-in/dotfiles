---
- name: recreate directory
  block:
    - include_tasks: '{{ helper_dir_remove }}'
      vars:
        path: '{{ sync_directory }}'

    - include_tasks: '{{ helper_dir_create }}'
      vars:
        path: '{{ sync_directory }}'
