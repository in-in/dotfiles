---
utility_link:
  src: '{{ dotfiles_roles }}/bin/files/bin'
  dest: '{{ user_home }}/bin'

package_apt_list:
  install:
    - 'ffmpeg'
    - 'libvips42'

package_pip_list:
  install:
    - 'pyvips'
