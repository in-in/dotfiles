---
utility_link:
  src: '{{ dotfiles_roles }}/bin/files/bin'
  dest: '{{ user_home }}/bin'

utility_package:
  apt:
    install:
      - 'ffmpeg'
      - 'libvips42'
  pip:
    install:
      - name: 'pyvips'
