---
- name: run copy helper
  import_tasks: '{{ helpers }}/copy.yml'
  vars:
    copy_list:
      - src: 'gpg-agent.conf'
        dest: '{{ user_home }}/.gnupg'
      - src: 'gpg.conf'
        dest: '{{ user_home }}/.gnupg'
