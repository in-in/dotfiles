---
utility_package:
  apt:
    install:
      - curl
      - dconf-editor
      - fonts-hack-ttf
      - gnome-shell-extension-dash-to-panel
      - gnome-tweaks
      - synaptic
      - tree
  pip:
    install:
      - name: glances

utility_gitignore:
  ignore:
    - '*.retry'
