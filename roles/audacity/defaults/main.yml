---
utility_repository:
  - repo: 'ppa:ubuntuhandbook1/audacity'

utility_package:
  apt:
    install:
      - 'audacity'

utility_copy:
  paths:
    - src: '{{ dotfiles_roles }}/audacity/files/.audacity-data'
      dest: '{{ user_home }}'
