---
utility_package:
  apt:
    install_recommends: no
    install:
      - 'audacity'

utility_copy:
  paths:
    - src: '{{ dotfiles_roles }}/audacity/files/.audacity-data'
      dest: '{{ user_home }}'
