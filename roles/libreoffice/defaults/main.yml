---
utility_package:
  apt:
    install:
      - 'libreoffice-calc'
      - 'libreoffice-writer'
      - 'libreoffice-style-sifr'

utility_copy:
  paths:
    - src: '{{ dotfiles_roles }}/libreoffice/files/libreoffice'
      dest: '{{ user_config }}'
