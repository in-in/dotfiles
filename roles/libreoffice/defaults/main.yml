---
utility_package:
  install:
    - 'libreoffice-calc'
    - 'libreoffice-writer'

utility_copy:
  dir: '{{ user_config }}/libreoffice'
  paths:
    - src: '{{ dotfiles_roles }}/libreoffice/files/libreoffice'
      dest: '{{ user_config }}'
