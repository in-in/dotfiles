- import_tasks: '{{ helpers }}/repo.yml'
  vars:
    repo_facts:
      - repo: 'ppa:wireguard/wireguard'

- import_tasks: '{{ helpers }}/package_apt.yml'
  vars:
    package_apt_facts:
      install:
        - 'wireguard'

- import_tasks: '{{ helpers }}/alias.yml'
  tags: 'alias_wireguard'
