---
- name: helpers | directory
  block:
    - name: helpers | directory | remove
      file:
        path: '{{ path }}'
        state: absent
    - name: helpers | directory | create
      file:
        path: '{{ path }}'
        state: directory
