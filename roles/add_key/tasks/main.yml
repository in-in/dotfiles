---
- name: create keys list
  set_fact:
    add_key_filename_list: '{{
      hostvars.localhost
      | json_query("*.add_key_filename")
      | list
    }}'

- name: add keys
  become: yes
  apt_key:
    url: '{{ item }}'
  with_items: '{{ add_key_filename_list }}'
