---
- name: install users packages
  command:
    'code --install-extension {{ item }}'
  register: command_result
  changed_when: not command_result.stdout | regex_search("(is already installed)")
  until: command_result.failed == false
  retries: 5
  delay: 10
  loop: '{{ vscode_extensions }}'
