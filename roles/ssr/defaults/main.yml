---
utility_package:
  apt:
    install:
      - 'simplescreenrecorder'

utility_template:
  - path: '{{ dotfiles_roles }}/ssr/templates'
    dest: '{{ user_home }}/.ssr'
    filename: 'settings.conf'
