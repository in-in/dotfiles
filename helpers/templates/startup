[Desktop Entry]
Comment={{ startup_list.name }}
Exec={{ lookup('pipe', 'which {{ startup_list.name }}') }} {{ startup_list.options | default(None) }}
Name={{ startup_list.name }}
Terminal=false
Type=Application
Encoding=UTF-8
X-GNOME-Autostart-Delay={{ startup_list.delay | default(30) }}
X-GNOME-Autostart-enabled=true
