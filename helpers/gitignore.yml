---
- name: helper | gitignore | add text block to the file
  blockinfile:
    create: yes
    marker: '# {mark} role: {{ role_name }}'
    path: '{{ dotfiles_directory }}/.gitignore'
    block: '{{ gitignore_facts }}'
