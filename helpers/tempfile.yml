---
- name: helper | tempfile | create temporary directory
  tempfile:
    state: directory
    suffix: '{{ tempfile_suffix }}'
  register: tempfile_result

- name: helper | tempfile | get directory path
  set_fact:
    helper_tempfile_result: '{{ tempfile_result.path }}'
